# v0.7.0 - Qualidade de código

## Alterado

| Item                   | Descrição                                                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Arquitetura em camadas | Código reorganizado em `main/`, `preload/`, `renderer/services/`, `renderer/ui/`. Cada arquivo com responsabilidade única   |
| `RoomClient` dividido  | A lógica de sala P2P foi separada em `MemberRegistry`, `MembershipGossip`, `MediaSharing`, `peerSession` e `iceDiagnostics` |
| Padrão de eventos      | Serviços de negócio não tocam mais em DOM. Comunicam com a interface via eventos                                            |
